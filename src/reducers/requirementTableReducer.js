//reducer for requirement table information.

export default function reducer(state={
  functionName: []
},action){
  //Add switch cases with type cases and payloads below.
  //switch(action.type){}
  return state;
}
